{
  "name": "codecraft",
  "version": "0.1.0",
  "description": "Codec Craft — in-browser WebM → GIF converter powered by ffmpeg.wasm",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1"
  },
  "devDependencies": {
    "vite": "^6.4.2"
  }
}
